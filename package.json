{
  "name": "veletest",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ veletest",
    "create-local": "graph create --node http://localhost:8020/ veletest",
    "remove-local": "graph remove --node http://localhost:8020/ veletest",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 veletest",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.43.0",
    "@graphprotocol/graph-ts": "0.29.1",
    "source-map-support": "^0.5.21"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
